# Clasificador de la calidad del vino

## Instalación de librerías y ejecución

Instalar las librerías necesarias para este proyecto con:

```pip install "fastapi[standard]" scikit-learn joblib pandas```

Ejecuta en modo desarrollo con:

```fastapi dev```

Ejecuta en modo producción con:

```fastapi run main.py```